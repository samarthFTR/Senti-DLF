# =============================================================================
# Senti-DLF — Python Dependencies
# Target runtime : Python 3.10 (Required for TF 2.10 native Windows GPU)
# Generated      : 2026-05-06
#
# Install (CPU-only, recommended for dev):
#   pip install -r requirements.txt
#
# Install (GPU on Windows natively):
#   conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0
#   pip install -r requirements.txt
# =============================================================================


# -----------------------------------------------------------------------------
# Deep Learning — TensorFlow
# TensorFlow 2.10 is the LAST version to support native Windows GPU training.
# -----------------------------------------------------------------------------
tensorflow<=2.10.1


# -----------------------------------------------------------------------------
# NLP — HuggingFace ecosystem
# transformers : DistilBERT tokenizer + TFDistilBertModel
# datasets     : Arrow-backed dataset loading (used in notebooks / eval)
# accelerate   : Mixed-precision & device-placement utilities
# evaluate     : Standardised metric computation (accuracy, F1, etc.)
# tokenizers   : Rust-backed fast tokenisers (pulled as a dep of transformers)
# -----------------------------------------------------------------------------
transformers>=4.38.0,<5.0.0
datasets>=2.18.0
accelerate>=0.29.0
evaluate>=0.4.0


# -----------------------------------------------------------------------------
# LoRA fine-tuning (PEFT)
# peft is not yet available for Python 3.13; pin to a known-good wheel.
# If peft is unavailable on your platform, LoRA layers can be implemented
# manually inside model.py — see docs/model_card.md.
# -----------------------------------------------------------------------------
peft>=0.10.0,<0.12.0


# -----------------------------------------------------------------------------
# Data & ML utilities
# Note: NumPy must be < 2.0 for compatibility with TF 2.10
# -----------------------------------------------------------------------------
scikit-learn>=1.3.2,<1.5.0 # stratified splits, baseline TF-IDF + LR
pandas>=2.0.0,<2.2.0       # DataFrame I/O for CSV / Parquet
numpy>=1.24.0,<2.0.0       # numerical ops
pyarrow>=14.0.0,<16.0.0    # Parquet engine (required by pandas.read_parquet)


# -----------------------------------------------------------------------------
# Backend — FastAPI inference server
# -----------------------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0  # ASGI server; [standard] adds watchfiles + httptools
pydantic>=2.7.0            # request / response schema validation
httpx>=0.27.0              # async HTTP client used in tests


# -----------------------------------------------------------------------------
# Configuration & environment
# -----------------------------------------------------------------------------
python-dotenv>=1.2.1       # loads .env into os.environ


# -----------------------------------------------------------------------------
# Dev & testing
# -----------------------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.25.0     # async test support for FastAPI routes
